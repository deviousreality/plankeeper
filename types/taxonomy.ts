export type FamilyOptions = {
  title: string;
  value: string;
  id: number;
};

export type GenusOptions = {
  title: string;
  value: string;
  id: number;
};

export type SpeciesOptions = {
  title: string;
  value: string;
  id: number;
};
